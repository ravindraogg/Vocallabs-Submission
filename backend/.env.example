# Deepgram API key (obtain from console.deepgram.com)
DEEPGRAM_API_KEY=your-deepgram-api-key

# Port to listen on (Render uses process.env.PORT automatically)
PORT=3001

# Allowed Frontend URL for CORS configuration (e.g. https://your-frontend.vercel.app)
FRONTEND_URL=http://localhost:3000
